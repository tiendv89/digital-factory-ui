# Path to the local clone of the management repo.
# The app reads feature and task YAML files from this directory at runtime.
# Example: /Users/yourname/workspace/project-workspace
WORKSPACE_MGMT_PATH=
