# Root directory containing workspace subdirectories (each with a workspace.yaml).
# The app scans one level deep for workspace.yaml files.
WORKSPACE_SCAN_ROOT=/path/to/workspaces

# Git commit identity for Server Actions that write back to status.yaml.
GIT_AUTHOR_NAME=your-name
GIT_AUTHOR_EMAIL=your-email@example.com

# SSH private key path used by simple-git for authenticated git push.
SSH_KEY_PATH=~/.ssh/id_ed25519
